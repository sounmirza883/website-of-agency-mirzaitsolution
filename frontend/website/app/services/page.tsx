import { Icon, PageHero, SiteShell } from "../components";
import { services } from "../data";
import { FadeIn, StaggerContainer, StaggerItem } from "../animations";

export default function ServicesPage() { return <SiteShell active="Services"><PageHero eyebrow="Services" title={<>Our Creative <strong>Digital Services</strong></>}>From design to development and marketing, Mirza IT Solution provides complete digital solutions to grow your brand.</PageHero><section><div className="container"><FadeIn><div className="color-block coral"><StaggerContainer className="grid-3 service-block-grid">{services.map((service) => <StaggerItem className="service-card" href={`/services/${service.slug}`} key={service.slug}><div className="icon"><Icon name={service.icon} /></div><h3>{service.title}</h3><p>{service.description}</p></StaggerItem>)}</StaggerContainer></div></FadeIn></div></section><section><FadeIn><div className="container"><div className="color-block navy text-center"><div className="section-eyebrow">Get Started</div><h2 className="section-title">Need a Complete <strong>Digital Solution?</strong></h2><p className="section-subtitle inverse">Mirza IT Solution can handle your design, editing, website, branding, and marketing needs in one place.</p><a href="/contact" className="pill pill-primary">Request a Free Quote <Icon name="fa-arrow-right" /></a></div></div></FadeIn></section></SiteShell>; }
