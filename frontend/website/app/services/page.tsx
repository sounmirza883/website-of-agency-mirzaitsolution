import { Icon, PageHero, SiteShell } from "../components";
import { serviceDetails } from "../data";
import { FadeIn } from "../animations";

export default function ServicesPage() { return <SiteShell active="Services"><PageHero eyebrow="Services" title={<>Our Creative <strong>Digital Services</strong></>}>From design to development and marketing, Mirza IT Solution provides complete digital solutions to grow your brand.</PageHero>{serviceDetails.map(([title, description, items, tone], index) => <FadeIn key={title}><section id={`service-${index + 1}`}><div className="container"><div className={`color-block ${tone}`}><div className="section-eyebrow">Service {String(index + 1).padStart(2, "0")}</div><h2 className="section-title">{title}</h2><p className="service-copy">{description}</p><ul className="checklist">{items.map((item) => <li key={item}><Icon name="fa-check-circle" />{item}</li>)}</ul><a href="/contact" className="pill pill-primary">Start Your Project <Icon name="fa-arrow-right" /></a></div></div></section></FadeIn>)}<section><FadeIn><div className="container"><div className="color-block navy text-center"><div className="section-eyebrow">Get Started</div><h2 className="section-title">Need a Complete <strong>Digital Solution?</strong></h2><p className="section-subtitle inverse">Mirza IT Solution can handle your design, editing, website, branding, and marketing needs in one place.</p><a href="/contact" className="pill pill-primary">Request a Free Quote <Icon name="fa-arrow-right" /></a></div></div></FadeIn></section></SiteShell>; }
